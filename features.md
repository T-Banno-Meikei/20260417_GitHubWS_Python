# Pomodoro Timer 実装機能一覧

## 1. 必須機能 (MVP)

1. タイマー表示
- 残り時間を mm:ss 形式で表示し、リアルタイムに更新する。

2. セッション種別切替
- 作業、短休憩、長休憩を切り替えられる。

3. 基本操作
- 開始、一時停止、再開、リセットを提供する。

4. 自動モード遷移
- 作業完了後に休憩へ、休憩完了後に作業へ遷移する。

5. 長休憩ルール
- 指定回数の作業完了ごとに長休憩へ遷移する。

6. 通知
- セッション完了時に音またはブラウザ通知を出す。

7. 設定変更
- 作業分数、短休憩分数、長休憩分数、長休憩間隔を変更できる。

8. 設定永続化
- 設定値を保存し、再訪時に復元する。

9. 状態復元
- リロード後もタイマー状態を可能な範囲で復元する。

10. レスポンシブUI
- スマートフォンとPCの両方で操作しやすい表示にする。

## 2. 推奨機能 (品質・運用)

1. 入力バリデーション
- 分数や間隔に上下限を設け、異常値を防止する。

2. エラーハンドリング
- 保存失敗時のメッセージ表示やフォールバックを実装する。

3. アクセシビリティ
- キーボード操作、フォーカス可視化、十分なコントラストに対応する。

4. 初期状態API
- バックエンドからデフォルト設定を取得できる。

5. ヘルスチェックAPI
- アプリ稼働確認用エンドポイントを提供する。

## 3. 将来拡張機能 (第2フェーズ)

1. セッション履歴保存
- 完了セッションを記録する。

2. 統計表示
- 日次、週次の作業時間や達成回数を表示する。

3. 自動開始設定
- 休憩自動開始、作業自動開始のON/OFFを提供する。

4. テーマ切替
- UIテーマや配色をカスタマイズ可能にする。

5. ユーザー管理
- ログインとユーザー単位の設定、履歴管理を行う。

## 4. テスト観点として重視する項目

1. タブ非アクティブ復帰時の時間整合性
2. 一時停止、再開での時間ずれ防止
3. 通知の多重発火防止
4. 境界値入力に対するバリデーション
5. API失敗時の安全なフォールバック動作
